<%= form_for(business) do |f| %>
  <% if business.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(business.errors.count, "error") %> prohibited this business from being saved:</h2>

      <ul>
      <% business.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fooditem %>
    <%= f.text_field :fooditem %>
  </div>

  <div class="field">
    <%= f.label :foodtype %>
    <%= f.text_field :foodtype %>
  </div>

  <div class="field">
    <%= f.label :expiration %>
    <%= f.date_select :expiration %>
  </div>

  <div class="field">
    <%= f.label :pickupstart %>
    <%= f.datetime_select :pickupstart %>
  </div>

  <div class="field">
    <%= f.label :pickupend %>
    <%= f.datetime_select :pickupend %>
  </div>

  <div class="field">
    <%= f.label :specialnote %>
    <%= f.text_area :specialnote %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
